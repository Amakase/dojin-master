<%# ─── Hero ──────────────────────────────────────────────────────────────── %>
<section class="home-hero">
  <div class="home-hero__noise"></div>
  <div class="home-hero__speed-lines"></div>
  <div class="home-hero__accent-line home-hero__accent-line--left"></div>
  <div class="home-hero__accent-line home-hero__accent-line--right"></div>

  <%# Bird's-eye convention floor plan — screen reader hidden %>
  <div class="home-hero__floor-art" aria-hidden="true">
    <%= render "pages/floor_art" %>
  </div>

  <%# Decorative convention atmosphere — screen reader hidden (4 tags, was 8) %>
  <div class="home-hero__bg-deco" aria-hidden="true">
    <span class="home-hero__bg-kanji home-hero__bg-kanji--1">即売会</span>
    <span class="home-hero__bg-kanji home-hero__bg-kanji--2">同人</span>
    <span class="home-hero__bg-kanji home-hero__bg-kanji--3">コミケ</span>
    <span class="home-hero__bg-tag home-hero__bg-tag--1">A-01a</span>
    <span class="home-hero__bg-tag home-hero__bg-tag--3">C107</span>
    <span class="home-hero__bg-tag home-hero__bg-tag--5">B-34b</span>
    <span class="home-hero__bg-tag home-hero__bg-tag--7">D-08a</span>
  </div>

  <div class="container home-hero__inner">

    <%# ── Left: hero copy ──────────────────────────────────────────────── %>
    <div class="home-hero__content">
      <p class="home-hero__eyebrow d-flex align-items-center gap-3">
        <span class="home-hero__eyebrow-jp">同人マスター</span>
        <span class="home-hero__eyebrow-divider">|</span>
        <span class="home-hero__eyebrow-en">Dōjin Master</span>
      </p>

      <h1 class="home-hero__title">
        Plan your <span class="home-hero__title-accent">perfect</span><br>
        convention experience.
      </h1>

      <p class="home-hero__subtitle">
        Track your favorite booths, prioritize what matters, and never miss a circle again —
        built for dōjin fans who show up prepared.
      </p>

      <div class="home-hero__cta d-flex flex-wrap gap-3 align-items-center">
        <% if user_signed_in? %>
          <%= link_to events_path, class: "btn home-hero__btn-primary text-decoration-none" do %>
            <i class="fa-solid fa-calendar-days me-2"></i>Browse Events
          <% end %>
        <% else %>
          <%= link_to new_user_registration_path, class: "btn home-hero__btn-primary text-decoration-none" do %>
            <i class="fa-solid fa-rocket me-2"></i>Get Started
          <% end %>
          <%= link_to new_user_session_path, class: "btn home-hero__btn-ghost text-decoration-none" do %>
            Sign In
          <% end %>
        <% end %>
      </div>
    </div>

    <%# ── Right: circle-cut showcase + upcoming events ───────────────────── %>
    <div class="home-hero__right">

      <%# Circle-cut image cross-fade carousel — one cover at a time %>
      <div class="home-circle-cuts" aria-hidden="true">
        <%# Ghost cards behind the active card for a "stack of covers" feel %>
        <div class="home-circle-cuts__ghost home-circle-cuts__ghost--back"></div>
        <div class="home-circle-cuts__ghost home-circle-cuts__ghost--mid"></div>
        <%# The six circle-cut cards (CSS cross-fades between them) %>
        <div class="home-circle-cuts__card">
          <%= image_tag "m3_circle_cuts/★_A-07a.jpeg", alt: "", draggable: "false" %>
          <span class="home-circle-cuts__label">A-07a</span>
        </div>
        <div class="home-circle-cuts__card">
          <%= image_tag "m3_circle_cuts/★_B-02b.jpeg", alt: "", draggable: "false" %>
          <span class="home-circle-cuts__label">B-02b</span>
        </div>
        <div class="home-circle-cuts__card">
          <%= image_tag "m3_circle_cuts/★_C-08a.jpeg", alt: "", draggable: "false" %>
          <span class="home-circle-cuts__label">C-08a</span>
        </div>
        <div class="home-circle-cuts__card">
          <%= image_tag "m3_circle_cuts/★_C-21a.jpeg", alt: "", draggable: "false" %>
          <span class="home-circle-cuts__label">C-21a</span>
        </div>
        <div class="home-circle-cuts__card">
          <%= image_tag "m3_circle_cuts/★_G-04a.jpeg", alt: "", draggable: "false" %>
          <span class="home-circle-cuts__label">G-04a</span>
        </div>
        <div class="home-circle-cuts__card">
          <%= image_tag "m3_circle_cuts/★_J-08b.jpeg", alt: "", draggable: "false" %>
          <span class="home-circle-cuts__label">J-08b</span>
        </div>
        <p class="home-circle-cuts__caption">
          <i class="fa-solid fa-images me-1"></i>Circle-cut art from recent events
        </p>
      </div>

      <%# Upcoming events panel %>
      <% if @upcoming_events.any? %>
        <aside class="home-events-panel" aria-label="Upcoming events">
          <p class="home-events-panel__label">
            <i class="fa-solid fa-calendar-days me-2"></i>Upcoming Events
          </p>

          <div class="home-events-panel__scroll">
            <% @upcoming_events.each do |event| %>
              <div class="home-event-card">
                <div class="home-event-card__dates">
                  <i class="fa-solid fa-calendar-day"></i>
                  <%= event.dates %>
                </div>
                <h3 class="home-event-card__name"><%= event.name %></h3>
                <p class="home-event-card__venue">
                  <i class="fa-solid fa-location-dot me-1"></i><%= event.venue %>
                </p>
                <% if user_signed_in? %>
                  <%= link_to event_path(event), class: "home-event-card__link stretched-link",
                        data: { turbo_frame: "_top" }, aria: { label: "View #{event.name}" } do %>
                    View <i class="fa-solid fa-arrow-right ms-1"></i>
                  <% end %>
                <% end %>
              </div>
            <% end %>
          </div>
        </aside>
      <% end %>

    </div>

  </div>

  <a href="#home-features" class="home-hero__scroll-hint d-flex flex-column align-items-center gap-2"
     aria-label="Scroll to features">
    <i class="fa-solid fa-chevron-down"></i>
  </a>
</section>

<%# ─── Features ──────────────────────────────────────────────────────────── %>
<section class="home-features" id="home-features">
  <div class="container">
    <p class="home-features__label">Everything you need</p>
    <h2 class="home-features__heading">Built for the convention floor.</h2>

    <p class="home-features__description">
      Dōjin (同人) are self-published works, including manga, novels, art books, and games that are created
      and sold by independent artists and circles at events like Comic Market. Dōjin Master helps you
      plan every visit so you never leave a favorite circle's table empty-handed.
    </p>

    <div class="row g-4 mt-2">

      <div class="col-12 col-md-4">
        <div class="home-feature-card">
          <div class="home-feature-card__icon-wrap d-inline-flex align-items-center justify-content-center">
            <i class="fa-solid fa-calendar-days home-feature-card__icon"></i>
          </div>
          <h3 class="home-feature-card__title">Browse Events</h3>
          <p class="home-feature-card__body">
            See upcoming conventions and keep up with your favorite participating circles.
            Find exactly what you're looking for before the gates open.
          </p>
        </div>
      </div>

      <div class="col-12 col-md-4">
        <div class="home-feature-card home-feature-card--featured">
          <div class="home-feature-card__icon-wrap d-inline-flex align-items-center justify-content-center">
            <i class="fa-solid fa-star home-feature-card__icon"></i>
          </div>
          <h3 class="home-feature-card__title">Favorite Booths</h3>
          <p class="home-feature-card__body">
            Mark booths with priority and add personal notes. Build your
            battle plan so you hit every must-visit table in time.
          </p>
        </div>
      </div>

      <div class="col-12 col-md-4">
        <div class="home-feature-card">
          <div class="home-feature-card__icon-wrap d-inline-flex align-items-center justify-content-center">
            <i class="fa-solid fa-book-open home-feature-card__icon"></i>
          </div>
          <h3 class="home-feature-card__title">Track Your Collection</h3>
          <p class="home-feature-card__body">
            See which works you already own at a glance. Avoid duplicates and
            keep track of new titles from circles you love.
          </p>
        </div>
      </div>

    </div>
  </div>
</section>
